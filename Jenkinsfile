pipeline {
    agent any 
    stages {
        stage('Build Docker Image') {
            steps {
                sh '''
                  echo "docker-compose build"
                '''
            }
        }
    }
}
